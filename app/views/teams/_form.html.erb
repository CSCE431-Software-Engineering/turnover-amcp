<%= form_with(model: team) do |form| %>
  <% if team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
        <% team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :team_number, style: "display: block" %>
    <%= form.number_field :team_number %>
  </div>

  <div>
    <%= form.label :member_count, style: "display: block" %>
    <%= form.number_field :member_count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
