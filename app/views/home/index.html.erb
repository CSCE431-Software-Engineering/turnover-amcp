<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AMCP</title>
  <style>
    .contact-header {
      background-color: #f2f2f2;
      padding: 20px;
      position: fixed; 
      bottom: 0; 
      left: 0; 
      width: 100%; 
      box-sizing: border-box; 
      text-align: center; 
    }

    .contact-header h2 {
      font-size: 20px;
      margin-bottom: 10px;
    }

    .contact-header p {
      margin: 0;
      font-size: 16px;
    }

    .editable-input {
      border: none;
      background: none;
      font-size: 16px;
      text-align: center;
    }

    .body-content {
      text-align: center;
      max-width: 600px; /* Set a maximum width */
      margin: 40px auto; /* Vertically space out and center the paragraph */
      padding: 20px;
      background-color: #f9f9f9; /* Light background color */
      color: #333333; /* Dark text for contrast */
      font-family: Arial, sans-serif; /* A readable font */
      line-height: 1.6; /* Spacing between lines */
      font-size: 18px; /* Comfortable reading size */
      border-radius: 8px; /* Optional: Rounded corners for aesthetics */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Optional: subtle shadow for depth */
    }

  </style>
</head>
<body>
<div style="text-align: center;">
  <h1>AMCP</h1>

  
  <% if @message %>
    <p><%= @message %></p>
  <% end %>
  <div class="body-content">
    <p>AMCP (Academy of Managed Care Pharmacy) is a professional association with the mission to ensure positive health care outcomes through quality, accessible, and affordable pharmaceutical care. Our organization aims to support our members with education, research, and the dissemination of health care information related to managed care pharmacy.</p>
  </div>
</div>
  <div class="contact-header">
    <h2>Contact Us</h2>
    <p>Name: Jeremiah Hall</p>
    <p>Email: <span id="email" contenteditable="true" class="editable-input"></span></p>
    <p>Phone: +1-123-456-7890</p>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", function() {
      const emailElement = document.getElementById('email');

      // Function to retrieve email from sessionStorage
      function getEmail() {
        const storedEmail = sessionStorage.getItem('email');
        if (storedEmail) {
          emailElement.innerText = storedEmail;
        } else {
          // Set default email if not found in sessionStorage
          emailElement.innerText = 'contact@amcp.com';
        }
      }

      // Call getEmail function when page loads
      getEmail();

      // Listen for changes in the email and store in sessionStorage
      emailElement.addEventListener('input', function() {
        const newEmail = this.innerText.trim();
        sessionStorage.setItem('email', newEmail);
      });
    });
  </script>
</body>
</html>




